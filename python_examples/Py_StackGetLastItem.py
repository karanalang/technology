stack = [0, 1, 2, 3, 4]

print(" python get item from top of list (i.e. last item) -> ", stack[-1])

print(" stack before removing last item -> ", stack)
print(" len of stack -> ", len(stack))
print("remove last item from stack -> ", stack.pop())

print(" stack after removing last item -> ", stack)